[defaults]

# this is only necessary to make the collection roles work from this directory,
# without the collection being installed (during development)
# See this for placing this collection correctly https://docs.ansible.com/ansible/latest/reference_appendices/config.html#collections-paths
# collections_paths=../../../..
collections_paths="/home/jveverka/git/ansible"
inventory=inventory

[ssh_connection]
ssh_args=-C -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=no
